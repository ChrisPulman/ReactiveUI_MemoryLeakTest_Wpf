<Window
    x:Class="ReactiveUI_MemoryLeakTest_Wpf.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:ReactiveUI_MemoryLeakTest_Wpf.ViewModels"
    d:DataContext="vm:MainViewModel"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid>
        <ItemsControl x:Name="TestIC" ItemsSource="{Binding Tests}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{Binding TestModel.Id}"></TextBlock>
                        <TextBlock Text="{Binding TestModel.Name}"></TextBlock>
                        <TextBlock Text="{Binding TestModel.Duration}"></TextBlock>
                        <TextBlock x:Name="tttt"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
